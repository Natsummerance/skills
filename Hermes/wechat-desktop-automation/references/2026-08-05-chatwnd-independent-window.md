# 2026-08-05 晚间：ChatWnd 独立聊天窗口 + 会话列表滚动 + 输入框 y 之谜解开

## 背景
发送链路反复失败（9 候选位置全失败、UIA 树一度无消息）——根因：**微信停在别的会话**（学校群「2025春歌剧欣赏」），不是二如亭主人。主窗口会话列表被置顶群占满，二如亭主人在置顶区第一位但列表停在群聊区。

## 会话列表滚动（验证过）
- mouse_event WHEEL 滚轮：**无效**（列表不动）
- keybd_event PgDn / PgUp（先真实鼠标点击列表中部聚焦）：**有效**，一次翻一屏
- 置顶会话 label 带「已置顶」后缀：`'二如亭主人已置顶'`、`'文件传输助手已置顶'`
- ListItem `selected: True` = 当前会话；聊天区跟随 selected
- 滚动列表本身会改 selected（PgDn 一次直接选中置顶区第一个 → 聊天区切换）
- 点击会话项物理坐标：frame 中心 → `(窗口left + cx/0.909, 窗口top + cy/0.909)`；二如亭主人 frame (360,156,374,96) → 物理 (880,287) ✓ 点击有效

## 输入框 y=937 之谜（解开）
- 聊天页打开时输入框在 UIA 树：Edit **label=会话名** frame (735,747,1269×95)
- 物理 y = top + 747/0.909 = 63+822 = 885（顶）→ 中心 937
- 输入框渲染区物理 ~885-989，**超出 GetWindowRect 窗口底边 913** → 点击 y=937 命中（okok 成功）
- 失败教训：9 候选 y=833-873 全在输入框顶 885 之上（点到消息区/工具区）
- **正确 y 扫描范围 = H-105 到 H-10**；输入框 x 范围 = frame x 换算（物理 1087-2483）

## ChatWnd 独立聊天窗口（用户操作：把二如亭主人单独点出来）
- 窗口签名：类名 `ChatWnd` + 标题 = 会话名（'二如亭主人'）；主窗口类是 `WeChatMainWndForPC`
- 实测枚举：`(32246956, 'ChatWnd', '二如亭主人', visible, (92,288,825×960))`
- **get_window_state 对 ChatWnd 返回 0 元素**（UIA 无暴露）——验证不能靠 UIA
- **SetForegroundWindow 被前台锁拦截**（返回前台 = 别的窗口 65966）
  - 解锁法 1：模拟 Alt（keybd_event 0x12 down/up）→ SetForegroundWindow
  - 解锁法 2（实测有效）：真实鼠标点击 ChatWnd 标题栏（(left+W/2, top+15)）→ 前台 = ChatWnd ✓
- 操作序列：MoveWindow 缩小 (92,150,825×600) 底边 750 → 点标题栏激活 → 点输入框 (left+0.55W, top+H-60) → SetClipboardData + Ctrl+V → Enter → 前台保持 ChatWnd ✓
- 发送结果验证：ChatWnd UIA 不可用 → 用户确认/截图

## 窗口尺寸漂移
MoveWindow 缩小后微信可能反向调整：1724×850 → 2586×1315（ensure_window 放大逻辑）→ 1373×929（End 键/布局变化？）。每次 GetWindowRect 重查，缩小后 1.5s 内可能被改回。

## 教训
1. **调试前先加载 wechat-desktop-automation 技能**（本会话被用户点名「你看看你之前沉淀的skill」）——技能已记录全部死路
2. **发送前确认 selected 会话 = 目标**——本会话反复失败最大根因
3. **测试消息铁律再次违反**（5 条「测试」进二如亭主人）——切到目标会话后也不该发测试，直接发真实回复

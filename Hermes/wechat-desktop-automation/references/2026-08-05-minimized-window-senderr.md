# 2026-08-05 深夜 SEND-ERR：最小化窗口 + 覆盖层 OCR 污染 完整排查

## 症状
20:09 cron 轮输出：
```
[SEND-ERR] 验证失败: 最后一条自己消息「夏天：你发啥呢」≠ 回复「好 关了」
二如亭主人说「我知道勒/把它关掉」→ 回复「好 关了」发送失败，保留待回复状态
```
随后手动 OCR 探针读到**非聊天内容**：「LLM/软件工程/工单详情/淘气兔/仪表盘/使用文档/我的订单/我的邀请」（y=16 起，标题栏区也有文字）——全是机场网页「淘气兔」的界面文字。

## 时间线
- 20:07 cron 成功回复「你发啥呢[捂脸]」（她发 bye/?/·.）→ state 更新
- 20:09 她发「我知道勒/把它关掉」→ bot 生成「好 关了」→ 发送 → 验证读回「夏天：你发啥呢」→ SEND-ERR → **state 不更新**（旧逻辑）→ 下轮会重发同批（重复发送风险）
- 20:11 手动暂停 cron
- 20:13 OCR 探针：WID=32246956 但截图区域读到淘气兔网页（**覆盖层内容**）
- 20:15 EnumWindows：`ChatWnd hwnd=32246956 标题="二如亭主人" rect=(-32000,-32000,-31175,-31400)` —— **窗口最小化**（经典坐标）

## 根因
用户当时正在电脑前活跃操作（浏览淘气兔网页、移动/最小化微信聊天窗）：
1. ChatWnd 被最小化 → 若 bot 继续点输入框，点击落在屏幕左上角/错误窗口
2. OCR 探针截图时 ChatWnd 被淘气兔网页覆盖 → BitBlt 读的是屏幕合成结果 = 覆盖层文字
3. 20:09 发送时窗口状态已异常 → 点击错位 → 「好 关了」大概率未发出 → 验证读到旧消息「夏天：你发啥呢」（20:07 回复，带「夏天：」前缀）

## 修复落地（watch_eruting.py）
1. `send_text_chatwnd`：发送前 `if H < 200 or r.left < -1000: return False`（最小化/异常放弃）
2. `main()`：失败也写 `reply_hash` + `last_failed=True`，同批不重发；成功 `pop("last_failed")`
3. 验证比较去名字前缀：`re.sub(r'^(夏天|二如亭主人)[:：]\s*', '', s)` 再去空白
4. 通知格式升级：成功 `🔔 已自动回复二如亭主人\n她说：「...」\n我回了：「...」`；失败 `⚠️ 自动回复失败（原因）... 已标记为已处理，不会重发同一批`

## 诊断口诀
- OCR 读到非聊天内容（网页/菜单/按钮文案）= 窗口被覆盖或已关 → 放弃本轮
- rect.left < -1000 = 最小化 → 放弃发送
- 用户活跃操作（窗口被移/最小化/覆盖）→ bot 让路：暂停 cron + 通知用户确认
